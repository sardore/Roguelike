import { hash } from './rng';
import { createWorld } from './world';
import { interactAt, move, useItem, wait } from './systems';
import type { GameState, Point } from './types';
const OBSERVATIONS:Record<string,string>={herbalist:'Most herb bundles have gone brittle. One is still green.',distillery:'The burners are cold. One copper coil is not.','north-alley':'A narrow strip through the grit has been swept clean.',glassworks:'The furnace is dark. The broken glass is warm.',courtyard:'The dry fountain basin smells faintly of cloves.','service-passage':'The pipes here knock once, then stay quiet.',underworks:'Condensation runs uphill along one pipe.','sealed-shop':'The boards over the inner passage were nailed from this side.'};
export class Game{state:GameState;listeners=new Set<()=>void>();constructor(seed='apothecaries-row'){this.state=createWorld(hash(seed))}sub(fn:()=>void){this.listeners.add(fn);return()=>this.listeners.delete(fn)}emit(){for(const fn of this.listeners)fn()}private observe(beforeX:number,beforeY:number){if(this.state.player.x===beforeX&&this.state.player.y===beforeY)return;const tile=this.state.tiles[this.state.player.y*this.state.width+this.state.player.x],room=tile?.room;if(room&&!this.state.enteredRooms.includes(room)){this.state.enteredRooms.push(room);const text=OBSERVATIONS[room];if(text){this.state.messages.push({text,tone:'odd'});if(this.state.messages.length>9)this.state.messages.shift()}}}move(dx:number,dy:number){const x=this.state.player.x,y=this.state.player.y;move(this.state,dx,dy);this.observe(x,y);this.emit()}wait(){wait(this.state);this.emit()}use(index:number,target?:Point){useItem(this.state,index,target);this.emit()}interact(target:Point){interactAt(this.state,target);this.emit()}restart(seed=`run-${Date.now()}`){this.state=createWorld(hash(seed));this.emit()}}
